[pytest]
pythonpath = .
testpaths = tests
markers =
    slow: Testes que demoram mais de 10 segundos
    integration: Testes de integração
    real_api: Testes que chamam API real
addopts = -v --tb=short --strict-markers